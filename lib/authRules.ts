// export function isEduEmail(
//     email?: string | null
// ): boolean {
//     return !!email &&
//         email.toLowerCase().endsWith("");
// }

export function isEduEmail(
    email?: string | null
): boolean {
    return !!email
}